| Claim | Verdict | Evidence | Correction |
| --- | --- | --- | --- |
| 1. Dependabot has 21 blocks, wildcard groups, and the stated ecosystem breakdown / ten uv wording. | FAIL | `.github/dependabot.yml:1-359`; parser output: `package-ecosystem blocks: 21`, breakdown `{'npm': 3, 'uv': 9, 'terraform': 4, 'github-actions': 1, 'gomod': 1, 'docker': 3}`. Uv blocks are at `.github/dependabot.yml:30,47,68,98,127,151,167,254,271`. Several blocks have no `groups:`: Terraform `.github/dependabot.yml:174-215`, gomod `.github/dependabot.yml:312-319`, Docker `.github/dependabot.yml:323-355`. Existing groups use wildcard list form, e.g. `.github/dependabot.yml:6-9`, not inline `patterns: ["*"]`; frontend also limits `update-types` to minor/patch at `.github/dependabot.yml:279-285`. | Say: `21 ecosystem blocks: 9 uv Python contexts, 3 npm, 4 Terraform, 3 Docker, 1 Go module, 1 GitHub Actions. Grouped blocks use wildcard patterns; several Terraform/Go/Docker blocks are ungrouped. Replace "ten uv" with "nine uv".` |
| 2. Listed ignore pins exist. | PASS | Training RL ignores marshmallow at `.github/dependabot.yml:54-62`. Training IL LeRobot ignores marshmallow, torch, numpy, packaging at `.github/dependabot.yml:75-92`. Evaluation ignores marshmallow, torch, numpy, packaging at `.github/dependabot.yml:105-122`. Evaluation SIL Docker uv ignores torch and av at `.github/dependabot.yml:134-145`. GitHub Actions ignores `github/gh-aw-actions` variants at `.github/dependabot.yml:222-232`. | — |
| 3. Test CI is ubuntu-latest CPU-only; pytest-training force-installs torch 2.11.0 while LeRobot lock pins torch 2.10.0; PR #958 / commit 36ba1ba are real and about the bump. | PASS | `grep -R "runs-on:" .github/workflows` showed all workflow runners as `ubuntu-latest` except Pester's matrix indirection; `.github/workflows/pester-tests.yml:32-37` resolves the matrix to `os: [ubuntu-latest]`. `.github/workflows/pytest-training.yml:19,29-41` runs on `ubuntu-latest`, Python 3.12, then `uv pip install torch==2.11.0`. `training/il/lerobot/uv.lock:2199-2200` pins `torch` `2.10.0`. `gh pr view 958` output: `security(deps): bump torch from 2.10.0 to 2.11.0 in /training/il/lerobot`, merged 2026-06-15. `git show --stat 36ba1ba`: `security(deps): bump torch from 2.10.0 to 2.11.0 ... (#958)`, changing `training/il/lerobot/requirements.txt`. | —; note the PR/commit prove the torch bump, but the CUDA/libcudart failure mechanism is not shown by `gh pr view` or `git show --stat` alone. |
| 4. Exactly one gh-aw workflow exists and it is slash-command, read-only, advisory. | PASS | `find .github/workflows -maxdepth 1 -name '*.md'` returned only `.github/workflows/aw-dependabot-pr-review.md`. Frontmatter has `engine: copilot` and `slash_command` at `.github/workflows/aw-dependabot-pr-review.md:1-9`; read-only permissions at `.github/workflows/aw-dependabot-pr-review.md:12-16`; safe outputs at `.github/workflows/aw-dependabot-pr-review.md:157-168`; advisory-only text at `.github/workflows/aw-dependabot-pr-review.md:173-175`. | —; note safe outputs are broader than add-comment: review comments, submit PR review, add-comment, and noop. |
| 5. Incident catalogue numbers and starlette churn are as described. | FAIL | #809 exists and matches: `gh issue view 809` says Python 3.11 container vs `>=3.12`, four cascading failures, created 2026-06-01 and closed 2026-06-10. #790 exists and matches the LeRobot OSMO Python 3.11.9 vs `>=3.12` failure, but its viewed body does not mention dropping `azureml-mlflow`; that detail appears in #809 against PR #541. #691 exists and matches the malformed ERE path-filter bug silently skipping fuzz tests. #547 exists and matches folder restructure/path-filter missed data-pipeline/training test failures. #346 exists, but is a Dependabot PR for 55 training updates, not release-please/uv.lock drift. Starlette churn is real: PRs #883/#884/#894 bumped 0.52.1→1.0.1 on 2026-06-04/05 and #981/#983/#984 bumped 1.0.1→1.3.1 on 2026-06-16. | Move the `azureml-mlflow` dropped detail from #790 to #809 or PR #541. Remove `release-please desynced uv.lock (#346)` unless another issue is cited. Say starlette moved `0.52.1→1.0.1→1.3.1` over about 11-12 days. |
| 6. Dependency churn is ~17/week recent, ~13/week all-time, ~210 all-time Dependabot PRs, six merged in one day. | FAIL | `gh pr list --search 'author:app/dependabot' --state all --limit 1000` returned 350 PRs total, first 2026-03-11 and latest 2026-06-22. Calculated all-time rate: 23.73/week; last six weeks: 169 PRs, 28.17/week. Merged total is 216, which is close to ~210 only if the claim means merged Dependabot PRs. Daily merged counts include more than six in one day, e.g. 22 on 2026-06-05, 13 on 2026-06-16, 10 on 2026-06-15. | Say: `~350 Dependabot PRs opened all-time (~216 merged); ~24/week opened all-time and ~28/week over the last six weeks; multiple days had six or more merged.` |
| 7. `8 runtime regressions reconstructed, 0 caught by green CPU CI`. | UNCERTAIN | The count basis is consistent with the catalogue if counted as four #809 cascading failures + #790 + #958 + #691 + #547 = 8. However, two are CI-integrity/path-filter failures, not runtime regressions, and `0 caught by green CPU CI` is analytical; the local repo and `gh issue/pr view` outputs do not independently prove every regression had green CPU CI. | Say: `Eight regressions/gaps reconstructed: four RL runtime failures, one LeRobot interpreter failure, one torch/CUDA bump, and two CI path-filter integrity failures; none is documented here as caught by CPU CI.` |
| 8. AzureML environment definitions and the RL train.yaml excerpt exist and match. | PASS | Files exist: `training/il/workflows/azureml/lerobot-train.yaml:20-31`, `training/il/workflows/azureml/components/train.yaml:15-24,48`, `training/il/workflows/azureml/components/preprocess.yaml:15-24,48`, `training/rl/workflows/azureml/train.yaml:51-61`. RL excerpt matches: `PYTHON`, `ACCEPT_EULA`, `PRIVACY_CONSENT`, `HYDRA_FULL_ERROR`, `TRAINING_CHECKPOINT_OUTPUT`, and the `Do NOT add ${{outputs.X}} env vars` comment are at `training/rl/workflows/azureml/train.yaml:53-61`. | — |
| 9. RL pins Python 3.11 + numpy 1.26.x; IL targets Python >=3.12. | PASS | `training/rl/pyproject.toml:5-11` says Isaac Lab container runtime is Python 3.11, `requires-python = "==3.11.*"`, and `numpy==1.26.4`. `training/il/lerobot/pyproject.toml:5` says `requires-python = ">=3.12"`. | — |
