# Verify B GPU claims

| Claim | Verdict | Evidence | Correction |
| --- | --- | --- | --- |
| 1. `ubuntu-latest` GitHub-hosted runner specs are 4 vCPU, 16 GB RAM, ~14 GB free SSD, no GPU. | PASS | GitHub docs list public-repo Linux `ubuntu-latest` / `ubuntu-24.04` as 4 CPU, 16 GB RAM, 14 GB SSD, x64. `gh repo view microsoft/physical-ai-toolchain` reports `PUBLIC`. The same docs list private-repo Linux `ubuntu-latest` as 2 CPU, 8 GB RAM, 14 GB SSD. Standard-runner tables have no GPU; GPU appears only under larger runners. | Say “for public repositories, `ubuntu-latest` is 4 vCPU / 16 GB RAM / 14 GB SSD storage; private repos get 2 vCPU / 8 GB. The 14 GB figure is documented storage, not necessarily measured free space.” |
| 2. `nvcr.io/nvidia/isaac-lab:2.3.2` is anonymously pullable and ~8.4 GB compressed. | PASS | Live NVCR check: anonymous token from `https://nvcr.io/proxy_auth?scope=repository:nvidia/isaac-lab:pull` returned HTTP 200. Manifest fetch for tag `2.3.2` returned HTTP 200, index with linux/amd64 digest `sha256:f07c37e3...`; linux/amd64 manifest returned 29 layers totalling 8,408,106,479 bytes = 8.408 GB decimal. | No correction. |
| 3. `evaluation/sil/docker/Dockerfile.lerobot-eval` uses a public Azure ML base, is CPU-runnable, and final size is roughly 7-9 GB because cu12 Torch is heavy. | PASS | Dockerfile line 1 uses `mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu22.04:20260609.v2@sha256:6796...`; live MCR manifest fetch returned HTTP 200. Dockerfile lines 7 and 28-29 copy `pyproject.toml` / `uv.lock` and install via `uv export --frozen --no-hashes --no-emit-project`. `evaluation/sil/docker/uv.lock` pins `torch==2.10.0` and cu12 packages (`nvidia-cuda-runtime-cu12`, `nvidia-cudnn-cu12`, etc.). Selected Linux cp312 Torch + cu12 + Triton wheels sum to about 4.106 GB compressed; MCR base manifest layers sum to about 0.330 GB before expanded layers and other Python deps. | Keep the size as approximate. Say “public MCR base; dependency graph is lock-derived; CPU execution should work with CUDA wheels, but the image still downloads about 4.1 GB of Torch/cu12/Triton wheels before expansion.” |
| 4. OSMO IL image is `pytorch/pytorch:2.11.0-cuda12.8-cudnn9-runtime`; AML IL uses a managed env; RL/OSMO uses `nvcr.io/nvidia/isaac-lab:2.3.2`. | PASS | `training/il/workflows/osmo/lerobot-train.yaml` line 160 sets default `image: pytorch/pytorch:2.11.0-cuda12.8-cudnn9-runtime`. `training/il/workflows/azureml/lerobot-train.yaml` line 30 uses `environment: azureml:lerobot-training-env:latest`; `training/il/scripts/submit-azureml-lerobot-training.sh` lines 197-214 register that Azure ML environment from an image. `training/rl/workflows/osmo/train.yaml` line 87 defaults to `nvcr.io/nvidia/isaac-lab:2.3.2`. | Minor precision: AML IL uses an Azure ML environment asset registered by the submission script, not a repository Dockerfile. |
| 5. RL import coupling differs: RSL-RL launches Isaac at module top level; SKRL defers Isaac import and is CPU-importable. | PASS | `training/rl/scripts/rsl_rl/train.py` imports `AppLauncher` at line 21, adds launcher args at line 79, parses at line 80, then instantiates `AppLauncher(args_cli)` at line 90 before later imports. `training/rl/scripts/skrl_training.py` imports Isaac training modules inside `_load_training_modules()` at lines 797-818 and imports `AppLauncher` inside `run_training()` at lines 1085-1093. | No correction. |
| 6. IL can run a real CPU step via `--policy.device=cpu`, but the lock pins cu12 Torch and no synthetic dataset fallback exists. | PASS | `training/il/scripts/lerobot/train.py` lines 506-533 forwards caller CLI args, checks `cli_text`, and only appends `--policy.device=cuda` when `--policy.device` is absent. The IL lock has `torch==2.10.0` at lines 2199-2200 plus cu12 runtime deps at lines 1174-1183 and torch deps on cu12 packages at lines 2210-2212. Searches found dataset CLI/root/repo handling but no synthetic dataset generator; `data_config.py` only builds dataset CLI args. | Say “one real CPU step is possible only if CI provides a small real LeRobot-format dataset; there is no committed synthetic fallback.” |
| 7. `--config-preview` exists on submission scripts but prints parsed values, not rendered workflow/job YAML. | PASS | `training/rl/scripts/submit-osmo-training.sh` documents `--config-preview` at line 55, parses it at line 148, and lines 174-193 print `Configuration Preview` key/value fields (`Task`, `Backend`, `Image`, `GPU`, `Workflow`, etc.) before `exit 0`. `training/il/scripts/submit-osmo-lerobot-training.sh` documents it at line 73, parses it at line 195, and lines 241-262 print key/value fields (`Dataset`, `Policy Type`, `Image`, `Workflow`, etc.) before exiting. | Replace “render + schema-validate YAML” with “print parsed config today; YAML rendering/schema validation would require new script behavior.” |
| 8. `jlumbroso/free-disk-space` reclaims ~20-31 GB to ~35-45 GB free; larger runners are 4-core/150 GB, 8-core/300 GB, 16-core/600 GB SSD. | FAIL | The action README says it “can clear up to 31 GB” and typical output reports `Saved 31GiB`; it does not substantiate a current 20 GB lower bound or a final 35-45 GB free range. Its sample starts with 31 GB available and would end around 62 GB, while current GitHub docs list standard `ubuntu-latest` storage as 14 GB. GitHub larger-runner docs do support 4 CPU / 16 GB / 150 GB SSD, 8 CPU / 32 GB / 300 GB SSD, and 16 CPU / 64 GB / 600 GB SSD. | Say “`jlumbroso/free-disk-space` claims up to 31 GB reclaimed; final free space is runner-image-dependent and should be measured in CI. Larger-runner disk SKUs are 4/150, 8/300, 16/600 GB.” |
| 9. Only GPU e2e catches CUDA/driver/Isaac-Vulkan/MIG breaks; no CPU RL training mode exists in the repo. | PASS | `docs/reference/gpu-configuration.md` lines 56-69 explain MIG `single` vs `none` and `CUDA_ERROR_NO_DEVICE`; lines 86-104 state Isaac Sim needs Vulkan and `NVIDIA_DRIVER_CAPABILITIES: "all"`; lines 122-128 show Vulkan initialization failure; lines 161-169 document Isaac shutdown workarounds on vGPU nodes. RL OSMO workflow requests `gpu: "1"` and sets `NVIDIA_DRIVER_CAPABILITIES: "all"` in `training/rl/workflows/osmo/train.yaml` lines 18 and 107. Repo search found `--headless` and distributed `cuda:<rank>` handling, but no supported CPU RL training path. | No correction beyond making the scope explicit: “no CPU RL training mode exists in this repository’s supported wrappers/workflows.” |
