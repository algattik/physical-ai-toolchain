# Sparse commit: only source + final deliverables are tracked.
# Tracked: gen_content.py, build_video.sh, PRESENTATION_SPEC.md, narration-script.md,
#          README.md, deck/presentation.pdf, presentation.mp4.
# Everything below is regenerated by the steps in README.md.

# Generator output (rebuild with gen_content.py)
content/
narration/
slides/

# TTS / encode caches
audio/
clips/
clips.txt
build_video.log
__pycache__/
*.aiff

# Built PPTX (~37 MB on the slim template) — rebuild it
deck/*.pptx

# Brand templates are large and not redistributable — see README.md
template/
*Global-Skilling*.pptx
*MSA-PPT-Template*.potx
